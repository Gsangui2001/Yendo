[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: todas las rutas sirven index.html para que React Router funcione
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
